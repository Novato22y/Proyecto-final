// main.js - Punto de entrada principal
import { initializeClock } from './core/clock.js';

document.addEventListener('DOMContentLoaded', () => {
    initializeClock();
    // Aquí puedes importar e inicializar otros módulos en el futuro
});
